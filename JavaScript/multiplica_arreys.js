const multiplicaArray = (arr, n) => arr.map(x => x * n);

const numeros = [1, 2, 3, 4, 5];
const multiplicador = 3;
const resultado = multiplicaArray(numeros, multiplicador);

console.log(resultado); 
